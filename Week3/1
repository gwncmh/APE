#include <iostream>
#include <vector>
using namespace std;
int main() {
    int n;
    cin>>n;
    vector<int> a(n);
    for(int i=0; i<n; i++) {
        cin>>a[i];
    }
    vector<bool> seen(n+1, false);
    for(int i=0; i<n; i++) {
        if(seen[arr[i]]) {
            cout << "Yes";
        }
        seen[arr[i]] = true;
    }
    cout << "No";
    return 0;
}
