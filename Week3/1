#include <iostream>
#include <vector>

int main() {
    int n;
    cin>>n;
    vector<int> arr(n);
    for(int i=0; i<n; i++) {
        cin >> arr[i];
    }
    vector<bool> seen(n+1, false);
    for(int i=0; i<n; i++) {
        if(seen[arr[i]]) {
            cout << "Yes";
        }
        seen[arr[i]] = true;
    }
    cout << "No";
    return 0;
}
